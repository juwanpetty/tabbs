import PropTypes from 'prop-types';

const WindowList = ({ windows }) => {
  return <div>WindowList content</div>;
};

export default WindowList;

WindowList.propTypes = {
  windows: PropTypes.node.isRequired,
};
